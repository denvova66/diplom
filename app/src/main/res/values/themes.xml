<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Market" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryVariant">@color/red_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="Theme.Market" parent="Base.Theme.Market" />

    <!-- Стиль без ActionBar для Login и Register -->
    <style name="Theme.Market.NoActionBar" parent="Theme.Market">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Стили для кнопок -->
    <style name="AutoRuButton" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:padding">8dp</item>
        <item name="cornerRadius">6dp</item>
        <item name="android:minHeight">36dp</item>
    </style>

    <!-- Компактный стиль для кнопок -->
    <style name="AutoRuButton.Small" parent="AutoRuButton">
        <item name="android:textSize">12sp</item>
        <item name="android:padding">6dp</item>
        <item name="android:minHeight">32dp</item>
        <item name="cornerRadius">4dp</item>
    </style>

    <!-- Стили для EditText -->
    <style name="AutoRuEditText" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edit_text_background</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/gray</item>
    </style>

    <!-- Стиль для Bottom Navigation -->
    <style name="BottomNavigationStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:background">@drawable/bottom_navigation_background</item>
        <item name="itemIconTint">@drawable/bottom_navigation_item_color</item>
        <item name="itemTextColor">@drawable/bottom_navigation_item_color</item>
        <item name="itemTextAppearanceActive">@style/BottomNavigationTextActive</item>
        <item name="itemTextAppearanceInactive">@style/BottomNavigationTextInactive</item>
    </style>

    <style name="BottomNavigationTextActive">
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="BottomNavigationTextInactive">
        <item name="android:textSize">11sp</item>
    </style>
</resources>